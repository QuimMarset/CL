func main ()

var x, a : int
var y, z : float
var b : bool
if not x == 2 then
    while not 3 > 4 do
        if (---4^(y + 3) > 0 == b) then
            x = 4^(x*4);
        endif
    endwhile
    if not -3 == -3 or (b or b and b) then
        z = (b^a*2)^x;
    endif
else
    x = 6/3;
endif
endfunc


